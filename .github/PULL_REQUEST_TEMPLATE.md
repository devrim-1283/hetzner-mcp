<!-- prettier-ignore-start -->

## What this changes

<!-- One paragraph. What behaviour is different after this merges? -->

## Why

<!-- The problem. Link an issue if one exists. -->

## Checklist

- [ ] `npm test` passes
- [ ] `npm run lint` and `npm run typecheck` pass
- [ ] `npm run codegen:check` passes, or the generated catalog is committed alongside
- [ ] No credential, connection name, or resource id from a real account appears in code, tests, or fixtures
- [ ] `CHANGELOG.md` has an entry under `[Unreleased]`, unless this is a no-op for users

## Tool surface

<!--
  Delete if this PR adds, removes, or renames nothing in the tool surface.
  Otherwise: removing or renaming a tool, or making an optional parameter required,
  is a BREAKING change under this project's versioning policy — see CHANGELOG.md.
-->

- [ ] No tool added, removed, or renamed
- [ ] Tool surface changed, and `CHANGELOG.md` labels it accordingly

<!-- prettier-ignore-end -->
